完美。现在所有决策点都明确了，我可以写一份完整的需求文档。

---

# API 网关路由管理系统 - 需求规格说明书

**版本**: 1.0  
**基于**: Octopus 项目扩展开发  
**目标用户**: 单用户（个人使用）

---

## 1. 项目概述

### 1.1 目标

构建一个 API 网关系统，允许用户通过统一的 Key 接入多个 AI 服务中转商渠道，并通过 Web 界面手动控制每个 Key 实际使用的渠道，同时支持自动故障转移。

### 1.2 核心价值

- **统一接入**：客户端配置一次 Key，永不修改
- **灵活切换**：后台一键切换渠道，无需重启客户端
- **独立控制**：不同设备/客户端使用不同 Key，互不干扰
- **自动容错**：当前渠道故障时自动切换，恢复后自动切回

### 1.3 技术栈

- **后端**: Go 1.21+
- **数据库**: SQLite
- **前端**: 现代 Web 技术栈（Vue 3 / React，具体待定）
- **部署**: 单二进制文件 + 内嵌前端资源
- **基础**: 复用 Octopus 的协议转换和故障转移能力

---

## 2. 核心概念

### 2.1 渠道 (Channel)

最底层的数据单元，代表一个中转商提供的具体接入点。

**属性**:
- ID: 唯一标识符（UUID）
- 名称: 用户自定义，如"中转商A - Claude特惠"
- Base URL: 上游 API 地址，如 `https://api.provider-a.com/v1`
- API Key: 上游密钥
- 协议类型: OpenAI / Anthropic / Gemini（自动检测或手动指定）
- 状态: 正常 / 异常 / 未知
- 备注: 可选，如"按次计费，便宜但不稳定"
- 创建时间 / 更新时间

**行为**:
- 可被多个 Router 引用
- 删除时需检查是否被 Router 使用，若被使用则禁止删除或提示解绑

### 2.2 路由器 (Router)

一组渠道的有序列表，加上路由模式配置。

**属性**:
- ID: 唯一标识符（UUID）
- 名称: 用户自定义，如"ClaudeRouter"
- 模式: 手动 / 自动
- 渠道列表: 有序的 Channel ID 数组
- 当前渠道 ID: 仅手动模式有效，指向当前激活的渠道
- 权重配置: 仅自动模式有效，JSON 格式 `{"channel_id": weight}`
- 故障转移开关: 布尔值，默认开启
- 创建时间 / 更新时间

**行为**:
- 手动模式：所有请求走"当前渠道"，故障时按列表顺序尝试下一个，原渠道恢复后自动切回
- 自动模式：按权重分配请求，故障渠道自动跳过
- 可被多个 Key 共享（可选功能，初版可不实现）

### 2.3 Key（接入凭证）

面向客户端的接入凭证，绑定到一个 Router。

**属性**:
- Token: 唯一字符串，格式 `sk-xxx`（32 字符随机）
- 名称: 用户自定义，如"MacBook Claude Code"
- 绑定的 Router ID: 外键
- 状态: 启用 / 禁用
- 备注: 可选
- 创建时间 / 更新时间

**行为**:
- 客户端使用此 Token 调用网关
- 网关根据绑定的 Router 决定请求转发到哪个 Channel
- 禁用后立即拒绝所有请求

---

## 3. 功能需求

### 3.1 渠道管理

#### 3.1.1 渠道列表页

**展示内容**:
- 表格形式，列：名称 / Base URL / 协议类型 / 状态 / 操作
- 状态显示：
  - 正常：绿色圆点 + "正常"
  - 异常：红色圆点 + "异常"
  - 未知：灰色圆点 + "未知"（新创建未测试的渠道）
- 操作列：编辑 / 删除 / 测试连接

**功能**:
- 点击"新增渠道"按钮打开创建表单
- 点击"测试连接"发送测试请求到该渠道，显示结果（成功/失败 + 延迟）
- 点击"删除"时检查是否被 Router 使用，若被使用则提示"该渠道正在被 X 个路由器使用，无法删除"

#### 3.1.2 渠道创建/编辑表单

**字段**:
- 名称（必填，文本输入）
- Base URL（必填，URL 输入，自动补全 `/v1` 后缀）
- API Key（必填，密码输入，编辑时显示为 `sk-***...***`，点击"显示"才完整展示）
- 协议类型（下拉选择：OpenAI / Anthropic / Gemini，默认 OpenAI）
- 备注（可选，多行文本）

**行为**:
- 保存时自动触发连接测试，测试失败时提示但仍允许保存
- 编辑时不允许修改协议类型（若需修改，删除重建）

#### 3.1.3 测试连接功能

**实现**:
- 发送一个最小成本的测试请求（如 `model: gpt-3.5-turbo, messages: [{"role":"user","content":"hi"}], max_tokens: 1`）
- 超时时间 15 秒
- 返回结果：成功（显示延迟）/ 失败（显示错误信息）

---

### 3.2 路由器管理

#### 3.2.1 路由器列表页

**展示内容**:
- 卡片形式，每个 Router 一张卡片
- 卡片内容：
  - 标题：Router 名称
  - 模式标签：手动 / 自动（不同颜色）
  - 渠道数量：如"包含 4 个渠道"
  - 当前状态：手动模式显示"当前：渠道名"，自动模式显示"自动分配"
  - 操作按钮：编辑 / 删除

**功能**:
- 点击"新增路由器"打开创建表单
- 点击卡片进入路由器详情页

#### 3.2.2 路由器详情页（核心页面）

**布局**:

```
┌─────────────────────────────────────────┐
│ 路由器名称: ClaudeRouter                  │
│ 模式: [手动] [自动]  (切换开关)            │
│ 故障转移: [✓] 开启                        │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ 渠道列表                                  │
│                                          │
│ ┌─────────────────────────────────────┐ │
│ │ 1. 混合渠道cc(别读pdf)                │ │
│ │    状态: ● 正常                       │ │
│ │    [切换到此]                         │ │
│ └─────────────────────────────────────┘ │
│                                          │
│ ┌─────────────────────────────────────┐ │
│ │ 2. 老的混合渠道cc          ← 当前    │ │
│ │    状态: ● 正常                       │ │
│ │    [当前使用中]                       │ │
│ └─────────────────────────────────────┘ │
│                                          │
│ ┌─────────────────────────────────────┐ │
│ │ 3. cc_pro_max(群主自用)               │ │
│ │    状态: ● 正常                       │ │
│ │    [切换到此]                         │ │
│ └─────────────────────────────────────┘ │
│                                          │
│ ┌─────────────────────────────────────┐ │
│ │ 4. 外部中转                           │ │
│ │    状态: ● 异常                       │ │
│ │    [切换到此] (灰色禁用)              │ │
│ └─────────────────────────────────────┘ │
│                                          │
│ [添加渠道] [保存顺序]                     │
└─────────────────────────────────────────┘
```

**手动模式行为**:
- 当前使用的渠道显示"← 当前"标记，按钮显示为"当前使用中"（禁用状态）
- 其他渠道显示"切换到此"按钮（蓝色，可点击）
- 异常状态的渠道按钮显示为灰色禁用状态，鼠标悬停提示"该渠道当前不可用"
- 点击"切换到此"后：
  - 立即更新"当前"标记
  - 弹出提示"已切换到：渠道名"
  - 无需刷新页面，按钮状态实时更新

**自动模式行为**:
- 不显示"切换到此"按钮
- 每个渠道后显示权重输入框（数字，0-100）
- 权重总和不强制为 100（系统自动按比例计算）
- 异常渠道自动跳过，不参与权重分配

**渠道排序**:
- 渠道卡片可拖拽调整顺序
- 顺序影响故障转移的尝试顺序
- 拖拽后点击"保存顺序"按钮生效

**添加渠道**:
- 点击"添加渠道"打开弹窗
- 弹窗内显示所有可用渠道的列表（排除已添加的）
- 多选后点击"确定"添加到路由器

#### 3.2.3 路由器创建/编辑表单

**字段**:
- 名称（必填）
- 模式（单选：手动 / 自动，默认手动）
- 故障转移（开关，默认开启）
- 初始渠道列表（可选，创建后可在详情页添加）

---

### 3.3 Key 管理

#### 3.3.1 Key 列表页

**展示内容**:
- 表格形式，列：名称 / Token / 绑定路由器 / 状态 / 操作
- Token 列默认显示为 `sk-***...***`，点击"复制"按钮复制完整 Token
- 状态：启用（绿色） / 禁用（灰色）
- 操作列：编辑 / 删除 / 启用/禁用切换

**功能**:
- 点击"新增 Key"打开创建表单
- 点击 Token 旁的"复制"按钮复制到剪贴板，显示"已复制"提示

#### 3.3.2 Key 创建/编辑表单

**字段**:
- 名称（必填，如"MacBook Claude Code"）
- 绑定路由器（下拉选择，必填）
- 备注（可选）
- Token（创建时自动生成，不可编辑；编辑时只读显示）

**行为**:
- 创建时自动生成 `sk-` 开头的 32 字符随机 Token
- 创建成功后弹窗显示完整 Token，提示"请妥善保存，关闭后无法再次查看完整 Token"

#### 3.3.3 接入信息展示

在 Key 详情页或列表页提供"接入信息"按钮，点击后弹窗显示：

```
接入地址: http://your-gateway.com/v1
API Key: sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

示例配置（OpenAI 格式）:
base_url: http://your-gateway.com/v1
api_key: sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

示例配置（Anthropic 格式）:
base_url: http://your-gateway.com
api_key: sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

### 3.4 请求代理

#### 3.4.1 请求流程

```
客户端请求
  ↓
验证 Token（从 Header 或 URL 参数提取）
  ↓
查询 Key 绑定的 Router
  ↓
根据 Router 模式决定目标 Channel
  ├─ 手动模式: 使用"当前渠道"
  └─ 自动模式: 按权重随机选择
  ↓
协议转换（复用 Octopus 逻辑）
  ↓
转发到目标 Channel
  ↓
返回响应（支持 SSE 流式）
```

#### 3.4.2 故障转移逻辑（手动模式）

```
请求到达当前渠道
  ↓
发送请求
  ↓
是否成功？
  ├─ 是: 返回响应，结束
  └─ 否: 判断错误类型
       ├─ 连接超时（15秒）: 触发故障转移
       ├─ HTTP 5xx: 触发故障转移
       ├─ HTTP 429（Rate Limit）: 触发故障转移
       ├─ 上游返回余额不足: 触发故障转移
       └─ 其他错误（4xx）: 直接返回错误，不转移
  ↓
标记当前渠道为"异常"
  ↓
按渠道列表顺序尝试下一个渠道
  ↓
所有渠道都失败？
  ├─ 是: 返回 502 Bad Gateway
  └─ 否: 返回成功响应
  ↓
后台定时检测（每 30 秒）
  ↓
原渠道恢复？
  ├─ 是: 标记为"正常"，下次请求自动切回
  └─ 否: 保持"异常"状态
```

#### 3.4.3 故障转移逻辑（自动模式）

```
按权重选择渠道
  ↓
排除"异常"状态的渠道
  ↓
发送请求
  ↓
失败时标记为"异常"，下次请求自动跳过
  ↓
后台定时检测恢复
```

#### 3.4.4 协议兼容性

复用 Octopus 的协议转换能力：
- 客户端发 OpenAI 格式 → 可转发到 Anthropic / Gemini 渠道
- 客户端发 Anthropic 格式 → 可转发到 OpenAI / Gemini 渠道
- 模型名映射通过 Octopus 的 Group 机制实现

---

### 3.5 日志记录

#### 3.5.1 请求日志

**记录内容**:
- 时间戳
- Key ID
- Router ID
- 目标 Channel ID
- 请求模型名
- 请求状态：成功 / 失败
- 失败原因（若失败）
- 是否触发故障转移
- 实际使用的 Channel ID（若发生转移）

**存储**:
- SQLite 表，保留最近 10000 条记录（超出后自动删除最旧的）
- 不记录请求内容和响应内容（隐私考虑）

#### 3.5.2 日志查询页面

**展示内容**:
- 表格形式，列：时间 / Key 名称 / 路由器 / 渠道 / 状态 / 详情
- 筛选条件：
  - 时间范围（最近 1 小时 / 24 小时 / 7 天）
  - Key（下拉多选）
  - 状态（成功 / 失败）
- 点击"详情"展开显示完整日志信息

**功能**:
- 实时刷新（可选，默认关闭）
- 导出为 CSV（可选功能，初版可不实现）

---

## 4. 数据模型

### 4.1 表结构

#### channels 表

```sql
CREATE TABLE channels (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    base_url TEXT NOT NULL,
    api_key TEXT NOT NULL,
    protocol TEXT NOT NULL, -- 'openai' | 'anthropic' | 'gemini'
    status TEXT NOT NULL DEFAULT 'unknown', -- 'normal' | 'error' | 'unknown'
    note TEXT,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL
);
```

#### routers 表

```sql
CREATE TABLE routers (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    mode TEXT NOT NULL, -- 'manual' | 'auto'
    channel_ids TEXT NOT NULL, -- JSON array: ["id1", "id2", ...]
    current_channel_id TEXT, -- 仅手动模式有效
    weights TEXT, -- JSON object: {"id1": 70, "id2": 30}, 仅自动模式有效
    failover_enabled INTEGER NOT NULL DEFAULT 1, -- 0 | 1
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL
);
```

#### keys 表

```sql
CREATE TABLE keys (
    id TEXT PRIMARY KEY,
    token TEXT NOT NULL UNIQUE,
    name TEXT NOT NULL,
    router_id TEXT NOT NULL,
    enabled INTEGER NOT NULL DEFAULT 1, -- 0 | 1
    note TEXT,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    FOREIGN KEY (router_id) REFERENCES routers(id)
);
```

#### request_logs 表

```sql
CREATE TABLE request_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp INTEGER NOT NULL,
    key_id TEXT NOT NULL,
    router_id TEXT NOT NULL,
    target_channel_id TEXT NOT NULL,
    model TEXT NOT NULL,
    status TEXT NOT NULL, -- 'success' | 'failed'
    error_message TEXT,
    failover_triggered INTEGER NOT NULL DEFAULT 0, -- 0 | 1
    actual_channel_id TEXT, -- 若发生故障转移，记录实际使用的渠道
    FOREIGN KEY (key_id) REFERENCES keys(id)
);

CREATE INDEX idx_request_logs_timestamp ON request_logs(timestamp);
CREATE INDEX idx_request_logs_key_id ON request_logs(key_id);
```

---

## 5. API 设计

### 5.1 RESTful API 端点

#### 渠道管理

```
GET    /api/channels          # 获取渠道列表
POST   /api/channels          # 创建渠道
GET    /api/channels/:id      # 获取渠道详情
PUT    /api/channels/:id      # 更新渠道
DELETE /api/channels/:id      # 删除渠道
POST   /api/channels/:id/test # 测试渠道连接
```

#### 路由器管理

```
GET    /api/routers           # 获取路由器列表
POST   /api/routers           # 创建路由器
GET    /api/routers/:id       # 获取路由器详情
PUT    /api/routers/:id       # 更新路由器
DELETE /api/routers/:id       # 删除路由器
POST   /api/routers/:id/switch # 切换当前渠道（手动模式）
```

**切换渠道 API 详细说明**:

```
POST /api/routers/:id/switch
Content-Type: application/json

{
  "channel_id": "uuid-of-target-channel"
}

Response 200:
{
  "success": true,
  "current_channel_id": "uuid-of-target-channel",
  "current_channel_name": "混合渠道cc"
}

Response 400 (渠道不在路由器中):
{
  "success": false,
  "error": "Channel not found in router"
}

Response 400 (非手动模式):
{
  "success": false,
  "error": "Router is not in manual mode"
}
```

#### Key 管理

```
GET    /api/keys              # 获取 Key 列表
POST   /api/keys              # 创建 Key
GET    /api/keys/:id          # 获取 Key 详情
PUT    /api/keys/:id          # 更新 Key
DELETE /api/keys/:id          # 删除 Key
POST   /api/keys/:id/toggle   # 启用/禁用 Key
```

#### 日志查询

```
GET    /api/logs              # 获取请求日志
  Query params:
    - start_time: Unix timestamp
    - end_time: Unix timestamp
    - key_id: 可选
    - status: 可选 ('success' | 'failed')
    - limit: 默认 100，最大 1000
```

#### 代理请求

```
POST   /v1/chat/completions   # OpenAI 格式代理
POST   /v1/messages           # Anthropic 格式代理
```

---

## 6. UI 设计要求

### 6.1 整体风格

- 现代简洁风格，参考 Tailwind UI / Ant Design / Shadcn UI
- 响应式布局，支持桌面端（主要）和平板端
- 深色模式支持（可选，初版可不实现）

### 6.2 导航结构

```
侧边栏导航:
├─ 仪表盘（可选，初版可不实现）
├─ 渠道管理
├─ 路由器管理
├─ Key 管理
└─ 请求日志
```

### 6.3 交互细节

- 所有删除操作需二次确认弹窗
- 表单验证实时反馈（输入框失焦时验证）
- 操作成功后显示 Toast 提示（右上角，3 秒后自动消失）
- 加载状态使用 Spinner 或骨架屏
- 错误信息清晰展示（如"连接超时：15 秒内未收到响应"）

### 6.4 关键页面原型

参考第 3 节功能需求中的布局描述，特别是路由器详情页的"切换到此"功能。

---

## 7. 技术实现要点

### 7.1 基于 Octopus 的改造

**复用部分**:
- 协议转换逻辑（`pkg/adapter`）
- 请求转发逻辑（`pkg/relay`）
- 故障检测逻辑（`pkg/provider` 中的健康检查）

**新增部分**:
- 数据库层（SQLite ORM，推荐使用 GORM）
- RESTful API 层（推荐使用 Gin 框架）
- 前端资源嵌入（使用 `embed` 包）
- Router 抽象层（管理 Channel 列表和路由逻辑）

### 7.2 关键逻辑实现

#### 手动模式的"自动切回"

```go
// 伪代码
func (r *Router) SelectChannel() *Channel {
    if r.Mode == "manual" {
        current := r.GetCurrentChannel()
        if current.Status == "normal" {
            return current
        }
        // 当前渠道异常，尝试下一个
        for _, ch := range r.Channels {
            if ch.Status == "normal" {
                return ch // 临时使用，不更新 current_channel_id
            }
        }
        return nil // 所有渠道都挂了
    }
    // 自动模式逻辑...
}

// 后台定时任务（每 30 秒）
func HealthCheckTask() {
    for _, channel := range allChannels {
        if channel.Status == "error" {
            if testConnection(channel) {
                channel.Status = "normal"
                channel.Save()
            }
        }
    }
}
```

#### 请求日志异步写入

```go
// 使用 channel 缓冲，避免阻塞请求
logChan := make(chan RequestLog, 1000)

go func() {
    for log := range logChan {
        db.Create(&log)
    }
}()

// 请求处理完成后
logChan <- RequestLog{...}
```

### 7.3 前端技术选型建议

- **Vue 3 + Vite + TypeScript**（推荐，生态成熟）
- UI 组件库：Element Plus / Ant Design Vue / Naive UI
- 状态管理：Pinia
- HTTP 客户端：Axios
- 构建产物嵌入到 Go 二进制中

---

## 8. 非功能性需求

### 8.1 性能

- 单机支持 100 QPS（个人使用足够）
- 请求延迟增加 < 50ms（相比直接调用上游）
- 数据库查询优化（索引、连接池）

### 8.2 可靠性

- 故障转移成功率 > 95%（假设至少有一个渠道可用）
- 数据库定期备份（手动或脚本）
- 日志轮转（保留最近 10000 条）

### 8.3 安全性

- API Key 在数据库中加密存储（AES-256）
- 前端不缓存完整 Token（仅显示脱敏版本）
- 无需用户认证（单用户，通过网络隔离保护）

### 8.4 可维护性

- 代码结构清晰，分层明确
- 关键逻辑有单元测试
- 配置文件与代码分离
- 日志分级（Info / Warn / Error）