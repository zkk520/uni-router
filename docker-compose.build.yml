services:
  uni-router:
    build:
      context: .
      dockerfile: Dockerfile
    image: uni-router:local
    container_name: uni-router
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
